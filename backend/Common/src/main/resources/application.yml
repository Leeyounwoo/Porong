server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://k6C102.p.ssafy.io:3308/common?characterEncoding=UTF-8
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
#      ddl-auto: create
    properties:
      hibernate:
       show_sql: true
       format_sql: true

  logging.level:
   org.hibernate.SQL: debug
   org.hibernate.type: trace



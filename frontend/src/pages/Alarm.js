import React from 'react';
import Header from '../components/Header';

export default function Alarm() {
    return (
        <div>
            <Header bback={true} name={ 'alarm' }/><br />
            알람 페이지 입니다.
        </div>
    )
}